apiVersion: argoproj.io/v1alpha1
kind: Rollout
metadata:
  name: project8-nginx-rollout
  namespace: project8-dev
spec:
  template:
    spec:
      containers:
        - name: nginx
          command: ["sh","-c","echo crashing intentionally; exit 1"]

